/** @type {import('next').NextConfig} */
const nextConfig = {
  serverExternalPackages: ['pdf-parse'],
  allowedDevOrigins: ['educator-ferret-sanction.ngrok-free.dev'],
};

export default nextConfig;
